
import Client from './Client'
export const metadata = { title: 'Import Reviews — Admin', description: 'Paste CSV and download reviews.json to place in /public/data.', robots: { index: false } }
export default function Page(){ return <Client/> }
